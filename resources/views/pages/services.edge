@layout()
  <section class="px-4 pb-10 pt-8 sm:px-6 lg:px-8 lg:pb-14 lg:pt-10">
    <div class="mx-auto max-w-7xl">
      <div class="grid gap-8 lg:grid-cols-[0.92fr_1.08fr] lg:items-center">
        <div class="max-w-[38rem]">
          <span class="inline-flex rounded-full border border-[#ddd9cf] bg-white px-4 py-2 text-[12px] font-semibold uppercase tracking-[0.2em] text-tindax-700 shadow-sm">
            Catalogue TindaX
          </span>
          <h1 class="mt-6 font-display text-4xl font-semibold leading-[1.05] tracking-[-0.04em] text-[#171717] sm:text-5xl lg:text-[4rem]">
            Tous les services disponibles a la livraison sur TindaX
          </h1>
          <p class="mt-5 max-w-2xl text-lg leading-8 text-slate-600">
            Repas, courses et petits colis: chaque service affiche ce qui est deja disponible dans le reseau TindaX pour une livraison rapide a Kinshasa.
          </p>

          <div class="mt-8 grid gap-4 sm:grid-cols-3">
            <div class="rounded-[1.6rem] border border-[#e7e0d5] bg-white px-5 py-5 shadow-panel">
              <p class="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Produits livrables</p>
              <p class="mt-3 font-display text-4xl font-semibold text-tindax-950">{{ totalProducts }}</p>
            </div>
            <div class="rounded-[1.6rem] border border-[#e7e0d5] bg-white px-5 py-5 shadow-panel">
              <p class="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Boutiques actives</p>
              <p class="mt-3 font-display text-4xl font-semibold text-tindax-950">{{ totalStores }}</p>
            </div>
            <div class="rounded-[1.6rem] border border-[#e7e0d5] bg-white px-5 py-5 shadow-panel">
              <p class="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Services</p>
              <p class="mt-3 font-display text-4xl font-semibold text-tindax-950">{{ services.length }}</p>
            </div>
          </div>

          <div class="mt-8 flex flex-col gap-3 sm:flex-row">
            <a
              href="#catalogue-services"
              class="inline-flex items-center justify-center rounded-full bg-tindax-700 px-6 py-3.5 text-sm font-semibold text-white transition hover:bg-tindax-800"
            >
              Explorer les services
            </a>
            <a
              href="{{ route('home') }}#hero-action"
              class="inline-flex items-center justify-center rounded-full border border-tindax-200 px-6 py-3.5 text-sm font-semibold text-tindax-900 transition hover:bg-tindax-50"
            >
              Demander une livraison
            </a>
          </div>
        </div>

        <div class="services-hero-panel">
          <div class="grid gap-4 md:grid-cols-3">
            @each(service in services)
              <article class="services-hero-card {{ service.accentClass }}">
                <p class="text-xs font-semibold uppercase tracking-[0.18em] text-slate-500">{{ service.eyebrow }}</p>
                <h2 class="mt-4 font-display text-2xl font-semibold tracking-[-0.03em] text-[#171717]">
                  {{ service.label }}
                </h2>
                <p class="mt-3 text-sm leading-7 text-slate-600">{{ service.description }}</p>
                <div class="mt-5 flex items-end justify-between">
                  <div>
                    <p class="text-sm font-medium text-slate-500">Disponibles</p>
                    <p class="mt-1 font-display text-3xl font-semibold text-tindax-950">{{ service.totals.itemCount }}</p>
                  </div>
                  <a href="#{{ service.anchor }}" class="text-sm font-semibold text-tindax-700">Voir</a>
                </div>
              </article>
            @end
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="catalogue-services" class="px-4 py-10 sm:px-6 lg:px-8 lg:py-14">
    <div class="mx-auto max-w-7xl">
      <div class="space-y-10">
        @each(service in services)
          <section id="{{ service.anchor }}" class="services-section">
            <div class="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between">
              <div class="max-w-3xl">
                <span class="inline-flex rounded-full bg-white px-4 py-2 text-[12px] font-semibold uppercase tracking-[0.18em] text-tindax-700 shadow-sm">
                  {{ service.eyebrow }}
                </span>
                <h2 class="mt-5 font-display text-4xl font-semibold tracking-[-0.04em] text-[#171717]">
                  {{ service.label }}
                </h2>
                <p class="mt-4 text-lg leading-8 text-slate-600">{{ service.description }}</p>
              </div>

              <div class="grid gap-3 sm:grid-cols-3">
                <div class="services-stat-card">
                  <p class="text-xs font-semibold uppercase tracking-[0.16em] text-slate-400">Produits</p>
                  <p class="mt-2 text-2xl font-semibold text-tindax-950">{{ service.totals.itemCount }}</p>
                </div>
                <div class="services-stat-card">
                  <p class="text-xs font-semibold uppercase tracking-[0.16em] text-slate-400">Boutiques</p>
                  <p class="mt-2 text-2xl font-semibold text-tindax-950">{{ service.totals.storeCount }}</p>
                </div>
                <div class="services-stat-card">
                  <p class="text-xs font-semibold uppercase tracking-[0.16em] text-slate-400">A partir de</p>
                  <p class="mt-2 text-2xl font-semibold text-tindax-950">
                    @if(service.totals.startingPrice !== null)
                      ${{ service.totals.startingPrice.toFixed(2) }}
                    @else
                      --
                    @end
                  </p>
                </div>
              </div>
            </div>

            <div class="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">
              @each(item in service.items)
                <article class="deliverable-card">
                  <div class="flex items-start justify-between gap-4">
                    <div>
                      <p class="text-xs font-semibold uppercase tracking-[0.18em] text-tindax-700">Disponible</p>
                      <h3 class="mt-3 font-display text-2xl font-semibold tracking-[-0.03em] text-[#181818]">
                        {{ item.name }}
                      </h3>
                    </div>
                    <span class="deliverable-price">${{ item.price.toFixed(2) }}</span>
                  </div>

                  <p class="mt-4 text-sm leading-7 text-slate-600">
                    {{ item.description || 'Article disponible a la livraison dans le reseau TindaX.' }}
                  </p>

                  <div class="mt-6 grid gap-3 rounded-[1.5rem] bg-[#f8f6f1] p-4">
                    <div class="flex items-center justify-between gap-4">
                      <span class="text-sm font-medium text-slate-500">Boutique</span>
                      <span class="text-sm font-semibold text-slate-900">{{ item.storeName }}</span>
                    </div>
                    <div class="flex items-center justify-between gap-4">
                      <span class="text-sm font-medium text-slate-500">Zone</span>
                      <span class="text-sm font-semibold text-slate-900">{{ item.commune }}</span>
                    </div>
                    <div class="flex items-center justify-between gap-4">
                      <span class="text-sm font-medium text-slate-500">Statut</span>
                      <span class="rounded-full bg-tindax-100 px-3 py-1 text-xs font-semibold uppercase tracking-[0.16em] text-tindax-700">
                        {{ item.status }}
                      </span>
                    </div>
                  </div>

                  <div class="mt-6 flex flex-col gap-3 sm:flex-row">
                    <a
                      href="{{ route('home') }}#hero-action"
                      class="inline-flex items-center justify-center rounded-full bg-tindax-700 px-5 py-3 text-sm font-semibold text-white transition hover:bg-tindax-800"
                    >
                      Demander ce service
                    </a>
                    <a
                      href="{{ route('home') }}#contact"
                      class="inline-flex items-center justify-center rounded-full border border-[#ded9cd] px-5 py-3 text-sm font-semibold text-slate-700 transition hover:bg-white"
                    >
                      Parler a l'equipe
                    </a>
                  </div>
                </article>
              @end
            </div>
          </section>
        @end
      </div>
    </div>
  </section>

  <section class="px-4 pb-14 pt-2 sm:px-6 lg:px-8 lg:pb-20">
    <div class="mx-auto max-w-7xl rounded-[2.4rem] bg-[#0b7b3f] px-7 py-10 text-white shadow-soft sm:px-10 lg:px-12">
      <div class="grid gap-8 lg:grid-cols-[1fr_auto] lg:items-center">
        <div>
          <p class="text-sm font-semibold uppercase tracking-[0.18em] text-gold-100">Besoin d'un article precis ?</p>
          <h2 class="mt-4 font-display text-4xl font-semibold tracking-[-0.04em]">
            TindaX peut vous aider a trouver ce qui est deja livrable dans votre zone.
          </h2>
          <p class="mt-4 max-w-3xl text-base leading-8 text-white/78">
            Dites-nous le produit, le repas ou le type de colis que vous souhaitez faire livrer et l'equipe TindaX vous oriente vers le bon service.
          </p>
        </div>

        <div class="flex flex-col gap-3 sm:flex-row lg:flex-col">
          <a
            href="{{ route('home') }}#hero-action"
            class="inline-flex items-center justify-center rounded-full bg-white px-6 py-3.5 text-sm font-semibold text-tindax-900 transition hover:bg-tindax-50"
          >
            Lancer une demande
          </a>
          <a
            href="{{ route('home') }}#contact"
            class="inline-flex items-center justify-center rounded-full border border-white/18 px-6 py-3.5 text-sm font-semibold text-white transition hover:bg-white/10"
          >
            Contacter TindaX
          </a>
        </div>
      </div>
    </div>
  </section>
@end
